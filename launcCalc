import smbus			#import SMBus module of I2C
from time import sleep          #import
import time
from mpu6050 import mpu6050
import math

accelerometer = mpu6050(0x68)

accelerometer_data = accelerometer.get_accel_data()


# find initial acceleration, set it as velocity
vxi,vyi,vzi = accelerometer_data.values()


start_time = time.time()
seconds = 1

while True:
	current_time = time.time()
	elapsed_time = current_time - start_time
	

	ax, ay, az = accelerometer_data.values()

	vx = (ax * .01) + vxi 
	vy = (ay * .01) + vyi
	vz = (az * .01) + vzi


	vnet = abs(math.sqrt(((vx)**2) + ((vy)**2) + ((vz)**2)))

	if elapsed_time > seconds:
		print("Done")
		break

print("vx :" + str(vx))
print("vy :" + str(vy))
print("vz :" + str(vz)) 
print("vnet :" + str(vnet))
